# Byte Studio — Static Website

This repository contains a static marketing site for Byte Studio.

Quick preview:

```bash
python -m http.server 8000
# open http://localhost:8000
```

Deployment options:
- Drag & drop `site.zip` to Netlify
- Push to GitHub and enable Pages
- Deploy to Vercel with `vercel` CLI

Screenshots: see `README-SCREENSHOTS.md` for instructions.
